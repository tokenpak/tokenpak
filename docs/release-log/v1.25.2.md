---
title: TokenPak v1.25.2 Release Log
version: 1.25.2
release_type: patch
semver_basis: compatible pricing and companion upgrade corrections
risk_class: full-conservative
validation_cadence: patch-focused-delta
changed_surfaces: python-proxy, companion, pricing, packaging, docs, workflow-release
status: in-progress
owner: TokenPak
started: 2026-09-07
closed: pending publication
---

# TokenPak v1.25.2 Release Log

## Scope

Proxy cost estimates now use explicit cache-read and cache-write prices from
the resolved model catalog entry. Previously, fixed multipliers overrode those
prices. Explicit zero-valued cache prices are honored. Input and output prices
come from the same resolved entry.

Ordinary Codex launcher upgrades reconcile unchanged legacy skill copies after
installing the canonical copies. Customized copies remain preserved, with
conflicts reported. An installation to a separate export directory keeps its
existing behavior.

The required license-policy check runs for both pull requests and main-branch
pushes. Missing or unusable comparison references fail before scanning; existing
file exclusions and the required status name remain unchanged.

## Compatibility and limitations

- The package advances from 1.25.1 to 1.25.2. No public API signature, telemetry
  schema, or TIP version changes. Exact normative docs and registry pins remain.
- Catalog data are unchanged. Cost figures remain rate estimates. Missing
  explicit cache prices retain their existing fallback estimates; unsupported
  long-lived cache-write and long-context price tiers are not added by this fix.
- Historical request costs and populated pricing databases are preserved. This
  release does not retroactively recalculate recorded costs.
- The separately distributed Pro package must declare compatibility with
  OSS 1.25.2 and TIP-1.0. Pro 0.2.0 declares OSS 1.25.1 only. The paired Pro
  0.2.1 artifact is validated and published separately before a paired rollout.
- Production Reroute recommendations still require complete measured inputs
  and eligible empirical priors. Synthetic tests and prepared comprehension
  displays do not supply empirical or human outcome evidence.

## Upgrade and rollback

Upgrade the OSS distribution to `tokenpak==1.25.2`. Existing Pro installations
also need the separately validated compatible Pro artifact. Check the installed
version, package origin and daemon compatibility before treating the pair as
ready. Custom settings and skills remain operator-owned.

Rollback restores the previous verified OSS 1.25.1 and compatible Pro 0.2.0
artifacts, with their existing settings and saved state. Keep backups until the
post-deployment watch completes. No tag is repointed and no published artifact
is replaced. The earlier 1.25.0 tag remains retired without a package release.

## Validation

| Check | State | Evidence |
|---|---|---|
| Source repairs | complete | Staging PRs #802 and #803 passed exact-head required and selected CI before integration. |
| Codex upgrade behavior | complete | 67 focused ownership, path and launcher checks passed. |
| Cache pricing and ledger | complete | 10 cache-price cases, 32 ledger/session-contract checks and 56 existing router/model-registry checks passed. Synthetic JSON and streaming exchanges assert independently calculated persisted costs and preserved wire bytes. |
| Version and generated metadata | complete | API snapshot parity passed with unchanged signatures; 204 version, metadata, endpoint and documentation checks passed. |
| Exact artifacts, clean install, upgrade and rollback | complete | The preparation commit produced one wheel and sdist. Normalization, metadata, package contents and leak checks passed. Clean install, 1.25.1 upgrade and rollback passed with configuration and all representative pricing rows preserved; installed CLI and loopback proxy checks passed. Final committed archives are rebuilt and reverified before integration. |
| Paired Pro compatibility | complete for the preparation pair | Pro 0.2.1 passed 85 affected checks and pinned CI lint. Installed OSS/Pro origins, exact declared bounds, a real daemon handshake and unlicensed command denial passed. Private hosted CI and publication follow OSS publication; rollout waits for both verified published artifacts. |
| License workflow event handling | complete | 24 focused workflow checks passed, including real Git comparisons for both event types, invalid references, excluded-only changes and the actual license scanner. |
| Current corrected candidate CI and final cut decision | preparing | Fresh checks and exact artifacts are required for the workflow correction before integration and publication. Earlier candidate results remain preparation evidence. |

Decision: **PREPARING**. The untagged candidate is being corrected so the public
release commit produces the required license result. Its final cut-time record
and exact artifacts follow current validation; publication has not occurred.

## Publication and observation

The release owner coordinates the identity-clean public fast-forward, immutable
annotated tag, native publication workflow, artifact verification, separate
docs/site propagation and rollout of the published pair. None is claimed by
this preparation record.

The patch watch retains the unfinished observation obligation from the first
published 1.25 release. The successor monitor must run through at least the later
of four hours after this patch's publication and 2026-09-08T19:06:08Z, with
stabilization checks and any gaps recorded. Publication is distinct from
deployment and closeout.
