---
title: TokenPak v1.25.0 Release Log
version: 1.25.0
release_type: minor
semver_basis: additive estimator verification and installed metadata, compatible pricing and forecast corrections, bounded retrieval, instruction preservation, and clearer session displays
risk_class: full-conservative
validation_cadence: minor-compact-delta
changed_surfaces: python-proxy, cli, companion, pricing, forecast, packaging, dashboard, docs
status: in-progress
owner: TokenPak
started: 2026-09-07T00:00:00Z
closed: pending publication
---

# TokenPak v1.25.0 Release Log

## Scope

The release adds `tokenpak savings --verify`, using an independent tokenizer
over a packaged fixture corpus. It reports divergence from the existing byte
estimator without changing recorded savings or claiming to recount requests
whose text was not retained.

Pricing catalogs include current model identifiers, explicit units, provenance,
and a preserve-first refresh path. Legacy numeric interpretations and existing
cost history remain intact. Forecast inputs preserve model and effort identity;
mixed histories and unsupported effort values remain unavailable for homogeneous
calibration. Session inactivity is not evidence of task completion.

Automatic vault retrieval has a configurable 2,000 ms deadline and a two-worker
bound. Timeouts and occupied-worker backlogs forward original request bytes and
produce explicit degradation receipts. All forwarding builders apply the same
internal-header predicate.

Companion and optional Pak-builder changes preserve role-bearing conversation
turns, isolate concurrent native launches, compose custom hooks, and retain
semantic journal provenance. Customized skills remain preserved during managed
upgrades. Session displays label estimates and distinguish guard runway from
session remainder.

Installed distributions carry canonical release metadata. The optional local
daemon must pass a bounded loopback health/version handshake with valid declared
compatibility before a client reports it active. Missing or incompatible daemon
metadata is shown as a diagnostic; the separately distributed Pro package is
not bundled into this OSS release.

## Compatibility and limitations

- The package version advances to 1.25.0. TIP-1.0 and its exact normative docs and
  schema pins remain unchanged.
- Pricing refresh preserves stored cost rows and old-date numeric interpretation.
  The existing reprocess pricing-version override remains ineffective.
- Forecast calibration, measured production reroute policies, and human
  comprehension outcomes require their own evidence. These source changes do
  not enable default-on rerouting or establish product-outcome claims.
- The existing NLTK advisory CVE-2026-81726 remains open for the optional
  `compression` and `llamaindex` extras; the current advisory lists no patched
  version. NLTK is not a base dependency.
- No breaking API or storage change is identified. The public API comparison
  with 1.24.0 adds 27 symbols and removes none; the telemetry schema is unchanged.

## Upgrade

```bash
python -m pip install --upgrade "tokenpak==1.25.0"
```

Install `tokenpak[tokens]==1.25.0` to use independent estimator verification.
Existing state and custom configuration are retained. Before upgrading a paired
daemon installation, install a Pro version whose declared compatibility includes
this OSS version and TIP-1.0. The prepared Pro 0.2.0 release declares exactly
OSS 1.25.0 and TIP-1.0. Wait for that separately published package before upgrading
a paired production installation; use the authenticated private index as usual.

## Rollback

```bash
python -m pip install --upgrade "tokenpak==1.24.0"
```

Keep the previous environment and configuration backup until validation and the
post-deploy watch finish. For paired installations restore Pro 0.1.6 with OSS
1.24.0. The isolated artifact upgrade and rollback retained all 19 representative
pricing rows, including a custom rate, and the exact configuration hash.

## Validation and release stages

The minor release round covers the merged source changes since 1.24.0. Its
initial run recorded 749 passed, 13 failed, six existing skips, and one known
expected failure. All 13 failures were fixed version assumptions in three test
modules; the corrected modules passed all 104 tests. All four actual native
Claude launch checks passed. Earlier failed results remain recorded.

The first hosted docs run found nine outdated version examples. Those examples
were corrected; 100 affected documentation tests passed. All 50 hosted checks
passed on preparation head `488d6c56a969be670717c074ca25e0860dbcf690`.
The final cut-record commit is separately checked at its exact head before
integration and publication; these preparation results are not relabeled as
future check runs.

The wheel and source archive passed metadata, packaged-data, exclusion and
public-leak checks. Installed-wheel smoke covered version/import provenance,
help, doctor, demo, estimator verification, startup and real loopback HTTP
forwarding. Fresh install, 1.24.0-to-1.25.0 upgrade and rollback passed. An initial
harness destination used an ineffective legacy environment variable on both
versions; the recorded correction used the supported absolute-form loopback
HTTP destination. That smoke does not establish configured provider routing or
user savings. Production payloads and final archive hashes are checked again
from the committed candidate before the tag.

Broad platform and human-outcome validation is deferred under the minor release
cadence. No demo is represented as a user's first measured savings receipt;
existing real-user value and comprehension gaps remain open. Directly changed
behavior, required hosted checks and artifact integrity remain pre-cut gates.

| Stage | Status | Evidence |
|---|---|---|
| Source integration | complete | Staging #786, #795, #796 and #787; source floor c3910074bdbc69b4705932bd418448142dd6402f. |
| Version and compatibility review | complete | Version 1.25.0, additive API comparison, unchanged telemetry schema and normative TIP pins; package metadata and declared daemon compatibility checked. |
| Release quality and artifact preparation | complete | Compact delta and affected corrections above; archive purity, installed-wheel upgrade and rollback passed. Final committed archive hashes are retained with publication evidence. |
| Staging validation and release-owner acceptance | complete | Preparation head 488d6c56a969be670717c074ca25e0860dbcf690 has 50 successful hosted checks. The release owner accepts the scoped change and documented limitations; final head checks are enforced at integration. |
| Public promotion | pending publication | Exact accepted shared-lineage commit will be recorded at promotion. |
| Tag, Release and PyPI | pending publication | Publication and matching distribution checksums will be recorded after the tag-push workflow. |
| Docs, site and runtime convergence | pending publication | Prepared documentation and separately published Pro pair accompany the rollout. |
| Observation and closeout | pending publication | The release owner is assigned to the 24-hour watch, with health observations no more than 30 minutes apart and a 6–18-hour stabilization sample. |

## Go / no-go

| Question | Answer | Evidence |
|---|---|---|
| Do applicable pre-cut technical checks pass? | Yes | Compact regression, corrected affected checks, snapshots and artifact preparation above; unchanged failed historical attempts are retained. |
| Does staging validation have zero unresolved failed checks? | Yes | All 50 hosted checks pass on the preparation head; final cut-record checks remain enforced before merge. |
| Are release notes complete and reviewed? | Yes | Source-grounded scope, compatibility, upgrade, rollback and known limitations are recorded. |
| Is rollback verified? | Yes | Restored 1.24.0 from the verified published wheel with representative state and configuration preserved. |
| Are breaking changes disclosed? | Yes | None identified; no API removals or storage migration, and Pro compatibility is explicitly bounded. |
| Is a watcher assigned? | Yes | The release owner owns publication, rollout, monitoring and closeout. |

Decision: **READY TO CUT**. This is the cut-time record. Publication requires the
final exact-head checks, accepted artifact hashes and live target-state checks;
it is not a claim that the tag, packages, deployment or watch already completed.
