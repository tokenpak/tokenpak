---
title: TokenPak v1.28.0 Release Log
version: 1.28.0
release_type: minor
semver_basis: compatible recorded-usage public contract and history repairs
risk_class: consequential
validation_cadence: compact-minor-with-focused-correctness
changed_surfaces: companion-history, economics-contract, calibration, packaging, docs
status: in-progress
owner: TokenPak
started: 2026-09-12
closed: pending publication
---

# TokenPak v1.28.0 Release Log

## Recorded usage and session history

Session economics can show the provider-observed usage subtotal and the number of
measured requests even when full-session totals are unavailable. The optional
`recorded_usage` field includes the observed subtotal, complete request count and
failed-request count. Terminal views label the subtotal and its coverage. Costs
derived from a rate card remain estimates; they are not provider bills.

Failed requests and missing measurements stay in full-session accounting and
forecast eligibility. Explicit recorded `xhigh` effort with supported provenance
forms its own forecast cell. Unknown or conflicting effort remains separate.
The release does not supply missing calibration history or numeric forecasts.
Shared line, block and dashboard views label forecast-derived guard runway as
an estimate, consistently with the companion footer and the observed-value legend.

Codex lifecycle hooks use Python's built-in SQLite support to register native
sessions and recover completed native turns. The intake preserves fork boundaries
and large transcript records; replay does not duplicate turns. Native transcript
files remain intact. Journal entries recover native identifiers, model metadata
and timestamps without inventing usage, cost or completion evidence. Session
history lists show actual journal entry counts.

## Upgrade and rollback

Back up configuration and state with consistent database backups and retain the
previous artifacts. Install `tokenpak==1.28.0`, preserving existing extras. The
standard service profile uses `tokenpak[serve,tokens,telemetry]==1.28.0`. Restart
the relevant services after active work finishes and use a new companion launch
to load updated hooks. Preserve running native client sessions.

Pro users also need the separately distributed Pro 0.4.3 package. It supports
OSS 1.26.0 through 1.28.0 with TIP-1.0. Pro 0.4.2 supports only OSS versions
through 1.27.0. Upgrade the pair together. The optional economics field is
compatible with existing v1 readers; the frozen usage schema and historical
ledger rows remain intact. No state migration is required by these changes.

For the standard service profile, roll back with:

```bash
python -m pip install "tokenpak[serve,tokens,telemetry]==1.27.0"
```

Retain any other extras used by the installation. Paired installations also
restore Pro 0.4.2 from its existing authenticated distribution before restarting
the relevant services. Preserve newly captured state and native transcripts
before restoring a backup; these changes require no state cleanup. Existing
public tags and artifacts are never replaced. No breaking changes or
deprecations are introduced by this release.

## Known limitations and security

Forecasts still require eligible homogeneous histories and scored observations.
Recovered journal turns and local fixtures do not establish calibrated forecasts,
independent outcomes or human comprehension. Missing provider measurements remain
unavailable. No provider session is automatically transferred or rerouted.

This release accepts the open NLTK
[GHSA-8mgp-746c-j5xp](https://github.com/advisories/GHSA-8mgp-746c-j5xp) and
Accelerate [GHSA-4j2p-28q2-5m79](https://github.com/advisories/GHSA-4j2p-28q2-5m79)
optional-dependency findings for this release only. Both are High under CVSS
v3.1; the Accelerate advisory separately lists Moderate severity under CVSS v4.
No verified published fix is available. The base install excludes both packages.
Optional integrations must avoid untrusted model paths and checkpoint repositories
and cannot rely on these APIs for filesystem containment. See
[SECURITY.md](https://github.com/tokenpak/tokenpak/blob/main/SECURITY.md) for affected extras and limitations. The findings
remain open and must be resolved or reassessed before another release; all other
checks remain required.

## Validation and completion

The release owner reviewed the complete change from 1.27.0, including journal
replay, fork isolation, request denominators, effort-cell identity, old-reader
compatibility and terminal presentation. The API comparison has eight additive
symbols and no removals. The telemetry-schema and workflow snapshots are unchanged.
TIP-1.0 and the pinned documentation and registry revisions remain compatible.

| Pre-cut check | Completed evidence |
|---|---|
| Source CI | All 50 native checks passed on `e6ce6c8e41a6f8556aded2b44d5c541be992ff48` in [the release candidate](https://github.com/tokenpak/tokenpak-staging/pull/827). The conservative classifier selected the full test set, including Python 3.10–3.13, packaging, storage, pricing, SDK, trust, integration and performance checks. |
| Focused behavior | 571 focused checks passed; two packaging-fixture errors were corrected and the entire affected file then passed all 19 checks. The merged presentation change passed 355 checks. The concurrent-health module passed all 19 checks with 60 successful requests and the existing 30-second aggregate bound retained. |
| Paired compatibility | Pro 0.4.3 passed 40 focused compatibility, endpoint and real-wheel checks, plus native CI on Python 3.10–3.12. Its supported OSS range is 1.26.0–1.28.0. |
| Build and install | The preparatory pair produced one wheel and one sdist per package; normalization, content purity, release scans and Twine checks passed. A separate clean installation passed CLI, doctor, compatibility, journal and local proxy checks. |
| Upgrade and rollback | Published 1.27.0 / Pro 0.4.2 artifacts were installed, upgraded, rolled back and restored. Journal/configuration state, previous readers, replay idempotence, entry counts, partial usage coverage and estimated-runway labels passed. Package dependency checks passed. |
| Documentation and identity | Strict MkDocs, documentation/version checks and public identity/trust checks passed. Earlier failed documentation-link and test-deadline checks are retained with their corrected results. |
| Security disposition | The two open optional findings disclosed above have release-specific acceptance. All other selected security checks pass. No affected optional package is present in the checked base service installations. |
| Evidence scope | Fixtures make no provider calls and establish no empirical calibration, first-time-user result or subscription billing. The broad first-value journey is deferred to a major-release assessment; changed correctness and required CI remain checked. |

Preparatory artifacts used source `520bb83897c8bcb94954fbf441b7e4509f85a792`
with the same runtime and dependency bytes as the tested source above. Their later
source differences are security/release documentation and the separately checked
concurrency test. This cut-time log completion changes documentation only. Exact
final-source archive, artifact, installed-content and native CI checks are enforced
again before promotion and tagging; preparatory artifact digests are not presented
as final release artifacts.

## Cut-time decision

| Question | Answer |
|---|---|
| Do applicable technical gates pass? | Yes, for the reviewed pre-cut source and evidence scopes above, with the disclosed optional-dependency disposition. Final-source enforcement remains mandatory. |
| Are any selected staging checks unresolved? | No. The corrected exact-head checks pass; earlier failures remain in the validation record. |
| Are the release notes complete and reviewed? | Yes. Scope, limitations, paired upgrade, rollback and security findings are disclosed. |
| Is rollback verified for these changes? | Yes. The published previous pair and state-preserving upgrade/rollback path passed. |
| Are compatibility changes disclosed? | Yes. Additive API only, unchanged stored schema, no breaking changes or deprecations, and an explicit paired compatibility range. |
| Is monitoring owned and active? | Yes. The release owner is actively watching the release and existing services. The successor plan requires a 24-hour OSS window, a separate 4-hour Pro window and preregistered technical samples after verified convergence. |

Decision: **READY TO CUT**. Reviewed by the TokenPak release owner at
2026-09-12 18:52 UTC. This decision does not assert publication or closeout.

| Post-cut stage | State |
|---|---|
| Public tag and package publication | pending publication |
| Published artifact and clean-install readback | pending publication |
| Documentation, site and runtime convergence | pending publication |
| Observation windows and release closeout | pending publication |
