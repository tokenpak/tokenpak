---
title: TokenPak v1.18.2 Release Log
version: 1.18.2
release_type: patch
status: in-progress
owner: TokenPak
reviewer: TokenPak
rollback_decider: TokenPak
started: 2026-08-05T07:20:33Z
closed:
---

# TokenPak v1.18.2 Release Log

## Scope

This compatible patch restores configured custom-provider loading, endpoint
routing, and outbound credential delivery; corrects compression/configuration
claims to match the default HTTP proxy; and adds conservative changed-surface
CI while pinning every third-party workflow action to a full peeled commit SHA
and requiring checksum verification before direct GitHub release downloads are
used, while repairing release-rehearsal dependency declarations. Validation
also identified and corrected prompt-path Companion persistence, monitor queue
shutdown, and telemetry schema initialization defects. The release includes
directly affected tests, documentation, version metadata, and generated release
snapshots.

Out of scope: pricing, billing, public or operator-run storage migrations, TIP
wire changes, unrelated feature work, and replacement of existing required
staging contexts. Private local SQLite plumbing is in scope and upgrades
additively and automatically.

## Sign-offs

| Role | Name | Decision | Timestamp |
|---|---|---|---|
| Release owner | TokenPak | In progress | 2026-08-05T07:20:33Z |
| Independent review | TokenPak | Pending exact-commit acceptance | |
| Rollback decider | TokenPak | Roll back to 1.18.1 on failed validation or health | 2026-08-05T07:20:33Z |

## Stage 0 — Change readiness

| Area | Status | Evidence |
|---|---|---|
| Scope and compatibility | Pass | Backward-compatible provider repair, truthful compatibility settings, additive CI and doctor JSON fields, repository-wide immutable workflow-action pins and digest-verified release fetches, plus validation-driven private persistence and writer-lifecycle corrections. |
| Focused implementation checks | Pass | Classifier, provider selection, real mock-upstream destination/path/query/body/credential delivery, credential non-overwrite/non-log, compression byte-fidelity, CLI, configuration, workflow topology, full-SHA action-pin, external-fetch checksum, and actionlint regressions passed before candidate freeze. |
| Full conservative validation | Pending | Exact candidate tests and artifacts are not complete. |
| Public identity and leak safety | Pending | Run against the immutable candidate before staging acceptance. |
| Documentation | In progress | Changelog, upgrade, rollback, limitations, and this release record are drafted. |

## Stage 1 — Integration preparation

- **Release candidate:** `df9f34d4a5f0a231acb701c10b307fdf852fdd27` (canonical `TokenPak <hello@tokenpak.ai>` author+committer throughout)
- **Staging pull request:** `tokenpak/tokenpak-staging#697`, open, based on `main` post-#696 (`f53b0f8eed...`)
- **Build output:** Locally rehearsed at the exact candidate head (not the shipped artifact -- governed `release.yml` rebuilds at tag push)
- **Artifact hashes (local rehearsal only):** wheel `95f2016092326a6b0603b384953131d73bd438592a37e7864bc81865c3adde25`, sdist `2304a2991422704a6dc619cac2568d5c497bb8a1954e0abf6f9fb710b72d7610`

## Stage 2 — Full-conservative validation

| Check | Result | Evidence |
|---|---|---|
| Always-on trust baseline | Pending | Blocked on a live GitHub Actions infrastructure outage (githubstatus.com indicator=major as of 2026-08-06T18:11Z) -- no workflow runs have started at the exact head; being watched for resolution, not forced. |
| Risk classifier and selected-job result | Pending | Same outage; not yet run at the exact head. |
| Existing required staging contexts | Pending | Same outage; not yet run at the exact head. |
| Python, proxy, and documentation suites | Rehearsed (locally + by 2 independent adversarial review passes) | tests/proxy/ 786 passed; tests/companion/+telemetry/ 934 passed; tokenpak/tests/test_custom_providers.py+test_proxy_config.py 58 passed; JS SDK 56 passed -- 0 unexpected failures across all runs at the exact head. Two independent review agents additionally ran these and adjacent suites themselves as part of adversarial verification (routing fix: reproduced the traversal exploit end-to-end + 36-case bypass battery, 0 bypasses; persistence fix: differential pre/post-fix testing on all 4 findings). Not a substitute for the governed CI run once the outage clears. |
| Node install, build, tests, and production audit | Rehearsed | `packages/tokenpak-js` at exact head: `npm run build` clean (tsc + ESM build script, the new addition this release adds); `npm test` -> 6 suites / 56 tests passed; `npm audit --production` -> 0 vulnerabilities. |
| Workflow and release-gate snapshots | Pending | |
| Wheel/source distribution build, metadata, contents, and purity | Rehearsed | Local build at exact head `df9f34d4a5f0a231acb701c10b307fdf852fdd27`: `twine check` PASSED both artifacts; zero Pro/`packages/` content in the wheel; zero `_internal/` paths in the sdist. SHA256 wheel `95f2016092326a6b0603b384953131d73bd438592a37e7864bc81865c3adde25`, sdist `2304a2991422704a6dc619cac2568d5c497bb8a1954e0abf6f9fb710b72d7610`. This is a local rehearsal, not the shipped artifact -- the governed `release.yml` build at tag push is still the binding gate. |
| Clean install and functional smoke | Rehearsed | Fresh venv install of the local wheel from a neutral cwd: `tokenpak --version` -> `tokenpak 1.18.2`; `tokenpak doctor` -> exit 0, 0 errors, 3 expected pre-setup warnings. |
| Upgrade from 1.18.1 | Rehearsed | Fresh venv: installed published `tokenpak==1.18.1` from PyPI, upgraded in place to the local 1.18.2 wheel. `--version` transitioned correctly; `doctor` exit 0 post-upgrade. |
| Rollback to 1.18.1 | Rehearsed | Same venv, reverse direction: `pip install --upgrade tokenpak==1.18.1` over the local 1.18.2 install. `--version` -> `tokenpak 1.18.1`; `doctor` exit 0 post-rollback. |
| Public API and compatibility review | Rehearsed | `scripts/release_gate/gen_api_snapshot.py --check` at the exact final head: "public-api snapshot matches on-disk". Symbol set unchanged. |

## Stage 3 — Go / no-go

| # | Question | Answer | Notes |
|---|---|---|---|
| 1 | Do all applicable technical gates pass on the exact release commit? | Pending | No staging merge before exact-head evidence. |
| 2 | Are all required and selected CI jobs successful and non-skipped? | Pending | Existing required contexts remain binding. |
| 3 | Is independent exact-head acceptance complete? | Pending | Separate acceptance is required. |
| 4 | Do release notes, limitations, upgrade, and rollback match the artifacts? | Pending | Recheck after build. |
| 5 | Is rollback to 1.18.1 verified? | Pending | Exercise with the exact artifacts. |
| 6 | Are breaking changes and migrations absent or disclosed? | Yes | No breaking change, public schema migration, or operator-run migration is required; private local stores upgrade additively in place. |
| 7 | Is the four-hour PATCH watcher ready? | Pending | Starts only after publication and deployment convergence. |

Decision: **NO-GO until every pending binding row is verified on the final exact head.**

## Upgrade

```bash
python -m pip install --upgrade "tokenpak==1.18.2"
```

No operator-run data migration is required. Private local Companion, monitor,
and telemetry stores apply backward-compatible plumbing upgrades automatically.

## Rollback

```bash
python -m pip install --upgrade "tokenpak==1.18.1"
```

Published artifacts are immutable. Deployment must retain a verified 1.18.1
environment and runtime-pointer backup until closeout.

## Compatibility disposition

- No breaking change is intended.
- No public data, configuration, registry, or storage-schema migration is
  required. Existing private local Companion, monitor, and telemetry databases
  apply idempotent additive columns and indexes automatically, preserving
  legacy cost rows while filling missing columns in place.
- The generated Python public-symbol set must remain unchanged.
- `tokenpak doctor --json` gains an additive `custom_providers` object with
  `configured`, `registered`, and `error` fields.
- Existing provider configuration continues to parse. Invalid provider entries
  remain non-fatal and are now distinguishable from successful registration.
  Routable custom providers may omit `models`; `api_key_env` names the optional
  outbound credential source. Any non-empty client upstream credential wins.

## Known limitations

- `TOKENPAK_COMPACT`, `compression.enabled`, and the threshold are compatibility
  settings for the legacy explicit helper. They do not activate body compaction
  in the built-in default HTTP proxy path.
- Risk-selected CI is additive for v1.18.2. Existing required staging contexts
  remain authoritative until separate parity evidence supports migration.
- Custom providers must use a supported built-in wire format. Endpoints with
  userinfo, fragments, or credential-shaped query parameters and invalid format
  entries are skipped and reported rather than partially registered. A
  configured endpoint query is treated as fixed routing policy and wins a
  same-name request query field.

## Stage 4 — Publication

| Substep | Timestamp | Output / URL |
|---|---|---|
| Final public source freeze | | |
| Annotated tag push | | |
| Release workflow | | |
| Build and checksums | | |
| GitHub Release | | |
| Package publication | | |
| Neutral clean-install verification | | |

## Stage 5 — Public propagation and deployment

Pending verified publication. GitHub, documentation, website, registry, and
runtime surfaces will be dispositioned separately; a copied version string does
not prove propagation. Each deployment host retains rollback anchors and must
pass source, import, CLI, service, health, functional, and optional paid-service
checks before the version floor advances.

## Stage 6 — Watch period

- **Watcher:** TokenPak
- **Watch start:** Pending publication and runtime convergence
- **Watch length:** 4 hours
- **Signals:** package/release digest agreement, public-surface convergence,
  proxy health and restart count, request errors, custom-provider registration,
  byte-fidelity regression, deferred/dropped local writes, queue-drain health,
  and deployed version-floor health

## Stage 7 — Closeout

- [ ] Release marked successful
- [ ] Changelog and shipped documentation reflect v1.18.2 behavior
- [ ] Public source, tag, Release, PyPI, assets, and checksums agree
- [ ] Applicable documentation, website, and registry surfaces are dispositioned
- [ ] Every canonical host reports the 1.18.2 floor and passes health checks
- [ ] Four-hour PATCH watch completed
- [ ] Independent closeout acceptance recorded
- [ ] Follow-up work and retained limitations recorded

Closed by: Pending

Closed at: Pending

## Exceptions / waivers

None. An unavailable required release channel, test, acceptance, publication,
propagation, deployment, or watch signal is a hold until a governed disposition
is recorded here.

## Attachments / evidence

Pending exact candidate SHA, staging/public integration SHAs, CI URLs, artifact
hashes, tag object and peel, publication provenance, deployment receipts, watch
samples, and independent acceptance.

## Retrospective

- **What dragged:** Pending closeout
- **What broke or nearly broke:** Pending closeout
- **Carry-forward action:** Pending closeout
