# Audit: monitor/ vs monitoring/ Modules — Pre-Merge Report

**Author:** Trix  
**Date:** 2026-03-25  
**Task:** TPK-MON-AUDIT-01  
**Status:** Complete — No merge needed

---

## Summary

> **TL;DR: The `tokenpak/monitor/` directory does not exist.** There is no merge required. `tokenpak/monitoring/` is already the sole canonical module. All imports throughout the codebase already reference `tokenpak.monitoring`.

---

## 1. Module Contents

### `tokenpak/monitor/`
```
DOES NOT EXIST
```
`find ~/vault/01_PROJECTS/tokenpak -type d -name "monitor"` → no results.

### `tokenpak/monitoring/` (canonical, 1854 total lines)
```
__init__.py        (21 lines)  — public API surface
audit_trail.py    (167 lines)  — AuditTrail class
health.py         (221 lines)  — HealthChecker, check_providers, get_cache_metrics, aggregate_status
metrics.py        (377 lines)  — ProxyMetricsCollector, ProviderMetrics, Prometheus helpers
provider_health.py(199 lines)  — ProviderHealthMonitor
request_logger.py (527 lines)  — RequestLogger, RequestLogRecord, log_request, new_request_id
request_size.py   (219 lines)  — RequestSizeMonitor, RequestSizeConfig, SizeAlert, get_monitor
server.py         (123 lines)  — ThreadedHTTPServer, MonitorHandler, run()
dashboard.html                 — Static HTML dashboard for monitoring UI
```

---

## 2. Import Graph (who uses monitoring/)

All imports already use `tokenpak.monitoring` — zero references to a `tokenpak.monitor` namespace:

| File | Imports |
|------|---------|
| `proxy_v4.py` | `RequestLogger` |
| `tokenpak/monitoring/audit_trail.py` | `AuditTrail` (self-referential, lazy) |
| `tokenpak/monitoring/metrics.py` | `ProxyMetricsCollector` (self-referential, lazy) |
| `tokenpak/_cli_core.py` | `monitoring.server.run` |
| `tokenpak/api/routes.py` | `HealthChecker`, `ProxyMetricsCollector` |
| `tokenpak/runtime/proxy.py` | `RequestLogger` |
| `tokenpak/agent/proxy/server.py` | `log_request`, `new_request_id`, `ProxyMetricsCollector` |
| `tokenpak/agent/proxy/server_async.py` | `request_size.get_monitor` |
| `tests/test_monitor.py` | `monitoring.server` (various) |
| `tests/test_prometheus_metrics.py` | monitoring metrics |

**No files import from `tokenpak.monitor`** (the old/missing namespace).

---

## 3. Duplicate Symbols

**None.** Since `tokenpak/monitor/` does not exist, there are no duplicate classes or functions to resolve.

### Public symbols in `tokenpak/monitoring/`
```
Classes:
  AlertLevel, AuditTrail, HealthChecker, MonitorHandler, ProviderHealthMonitor,
  ProviderMetrics, ProxyMetricsCollector, RequestLogger, RequestLogRecord,
  RequestSizeConfig, RequestSizeMonitor, SizeAlert, ThreadedHTTPServer

Functions:
  aggregate_status, check_providers, get_cache_metrics, get_monitor (×2 overloads),
  log_request, new_request_id, record_provider_request, reset_monitor, run
```

---

## 4. Recommendation

**No merge needed.** The prior task (TPK-VERSION-SYNC or equivalent) already resolved this.

- ✅ **Canonical module:** `tokenpak.monitoring` (keep as-is)
- ✅ **Old stub:** `tokenpak/monitor/` — already absent, nothing to delete
- ✅ **All imports:** Already correct throughout codebase

**Recommended follow-up for P1 task TPK-AUDIT-MONITOR-01:**
- Close or mark as `done` — the merge task's prerequisite condition is already satisfied
- Optionally run `grep -r "tokenpak.monitor[^i]"` periodically as a lint guard to catch any future regressions where someone accidentally creates a `monitor/` stub

---

## 5. Migration Steps Needed

**None.** No migration required.

---

## 6. Evidence

```bash
$ find ~/vault/01_PROJECTS/tokenpak -type d -name "monitor"
(no output — directory does not exist)

$ grep -r "from tokenpak.monitor[^i]" packages/core/ --include="*.py" | grep -v __pycache__
(no output — no bare monitor/ imports)
```

All 14 import sites confirmed pointing to `tokenpak.monitoring`.

---

*Generated by Trix heartbeat cycle 2026-03-25 20:55 PST*
