---
"tokenpak": minor
---

Add compatible authenticated native guard/workload observations, optional durable
budget accounting and workload-conditioned pricing quotes. Preserve existing
scalar pricing calls, default-off durable accounting and unsupported-evidence
refusals.

The public snapshot adds 36 bindings across pricing, native endpoint and
accounting modules, including their imported helper bindings. All existing
snapshot symbols remain; no public symbol removal is requested. Native response
schemas are explicitly versioned, and TIP-1.0 remains unchanged.
