---
---

Add the deterministic session-economics trip computer to the default read
surfaces: a one-line and full-block rendering in status, a dashboard section
and snapshot field, and a read-only companion tool shared by both supported
agent clients — all thin adapters over one validated contract and one shared
renderer, with proxy-owned default-session selection and a regression suite
proving the surfaces change no provider bytes and no accounting inputs
across a full process restart.
