---
---

Release-gate: public-API snapshot update for v1.18.3.

Additive change only. The companion MCP package gains `active_tools`
(profile-aware advertised-tool selection), captured in
`tokenpak.companion.mcp.tools` and re-exported by
`tokenpak.companion.mcp.server`. No symbol is removed or changed.
The release was explicitly classified patch by the operator with the
additive-symbol evidence on record.
