apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tokenpak-pvc
  namespace: tokenpak
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # Uncomment to specify a storage class:
  # storageClassName: standard
