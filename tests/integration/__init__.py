"""TokenPak Integration Tests

These tests verify TokenPak works end-to-end with real SDK adapters and frameworks.
They use mocked API calls (not live services) but exercise real adapter code paths.
"""
