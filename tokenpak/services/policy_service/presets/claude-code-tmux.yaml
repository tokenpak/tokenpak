# claude-code-tmux — Claude Code running inside a tmux session.
# Behaves identically to TUI; the separate preset exists for telemetry
# grouping (multi-session workspaces benefit from a distinct bucket).

body_handling: byte_preserve
cache_ownership: client
injection_enabled: false
injection_budget_chars: 2000
injection_min_query_tokens: 50
dlp_mode: warn
compression_eligible: false
ttl_ordering_enforcement: true
profile: claude-code-tmux
capture_session_id_header: x-claude-code-session-id
