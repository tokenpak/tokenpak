"""
tokenpak._internal — Non-public implementation modules.

These modules are internal implementation details. The underscore prefix
signals that they are NOT part of the public API. External code should
not import from here directly.

Subdirectories:
    agentic/    — PRO-gated agentic features
    auth/       — Authentication and API key management
    debug/      — Debug utilities
    fingerprint/— Request fingerprinting
    ingest/     — Schema ingestion and conversion
    license/    — License validation and feature gates
    macros/     — Prompt macro expansion
    memory/     — Session memory and capsule management
    query/      — Query models and processing
    regression/ — Regression detection
    state_schemas/ — State schema definitions
    teacher/    — Learning/teacher pipeline
    team/       — Multi-agent team coordination
    triggers/   — Event triggers
"""
