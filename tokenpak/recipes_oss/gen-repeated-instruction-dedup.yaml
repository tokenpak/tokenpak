name: gen-repeated-instruction-dedup
category: general
description: Deduplicate repeated instruction blocks (e.g. repeated system-prompt fragments).
pattern:
  match: any
action:
  operations:
  - type: dedup_paragraphs
    min_length: 80
    similarity_threshold: 0.95
  compression_hint: 0.2
