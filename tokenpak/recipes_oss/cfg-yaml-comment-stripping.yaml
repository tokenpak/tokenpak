name: cfg-yaml-comment-stripping
category: config
description: Strip all comments from YAML files and normalize indentation.
pattern:
  match: extension
  extensions:
  - .yaml
  - .yml
action:
  operations:
  - type: yaml_strip_comments
  - type: yaml_normalize_indentation
    indent: 2
  compression_hint: 0.15
