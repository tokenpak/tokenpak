name: py-exception-handler-compression
category: python
description: Compress verbose except blocks to a single summary line.
pattern:
  match: extension
  extensions:
  - .py
action:
  operations:
  - type: python_compress_except_blocks
    keep_first_line: true
    stub_body: '    ...'
  compression_hint: 0.15
