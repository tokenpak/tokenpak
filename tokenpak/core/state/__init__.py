"""Core state subpackage (Architecture §1 — shared runtime state).

    manager — StateManager: compact session state that persists across turns.
"""

from __future__ import annotations

from tokenpak.core.state.manager import *  # noqa: F401,F403
