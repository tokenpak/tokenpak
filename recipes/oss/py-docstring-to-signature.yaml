name: py-docstring-to-signature
category: python
description: 'Reduce docstrings to first summary line; configurable modes: keep_summary|trim|remove.'
pattern:
  match: extension
  extensions:
  - .py
action:
  operations:
  - type: python_docstring_compress
    mode: keep_summary
  compression_hint: 0.3
