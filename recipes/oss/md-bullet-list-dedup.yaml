name: md-bullet-list-dedup
category: markdown
description: Remove duplicate bullet list items within the same list block.
pattern:
  match: extension
  extensions:
  - .md
  - .markdown
  - .mdx
action:
  operations:
  - type: md_dedup_list_items
    scope: same_block
  compression_hint: 0.1
